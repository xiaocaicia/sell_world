// 导入 Vue
import Vue from 'vue'

// 导入 Button,Swipe 组件
import { Grid, GridItem,Button,Swipe, SwipeItem } from 'vant'

// 挂载 Button,Swipe 组件
Vue.use(Button).use(Swipe).use(SwipeItem).use(Grid).use(GridItem);

